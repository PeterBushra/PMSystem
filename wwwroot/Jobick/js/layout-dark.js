(function($) {
    "use strict"

    // set explicit Bootstrap theme for dark layout
    document.documentElement.setAttribute('data-bs-theme', 'dark');

    new dlabSettings({
        version: "dark"
    });


})(jQuery);